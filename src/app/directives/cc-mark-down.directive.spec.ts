import { CcMarkDownDirective } from './cc-mark-down.directive';

describe('CcMarkDownDirective', () => {
  it('should create an instance', () => {
    const directive = new CcMarkDownDirective();
    expect(directive).toBeTruthy();
  });
});
